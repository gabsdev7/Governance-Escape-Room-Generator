# ============================================
# Governance Escape Room - Environment Variables
# ============================================

# MCP Configuration
# -----------------
# The MCP server endpoint for fetching best-practice hints
# Default: https://learn.microsoft.com/api/mcp
MCP_ENDPOINT=https://learn.microsoft.com/api/mcp

# Optional: API key for MCP authentication (if required)
# Note: This is passed per-request and never persisted
# MCP_API_KEY=

# Hint Provider Selection
# Options: 'mcp' (default) or 'local'
# 'mcp' - Uses Microsoft Learn MCP endpoint with local fallback
# 'local' - Uses only local curated hints (for offline/testing)
HINT_PROVIDER=mcp

# Telemetry Configuration
# -----------------------
# Enable telemetry hooks for usage tracking (default: false)
# WARNING: When enabled, events are logged for analytics
NEXT_PUBLIC_ENABLE_TELEMETRY=false

# Azure Static Web Apps
# ---------------------
# These are typically set automatically by Azure SWA
# AZURE_STATIC_WEB_APPS_API_TOKEN=
